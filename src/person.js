export default class Person {
  constructor(name, age){
    this.name = name;
    this.earthAge = age;
  }

  galacticAge(planet, age) {
    // this.mercuryAge = parseFloat((this.earthAge * (1/0.24)).toFixed(1));
    // this.venusAge = parseFloat((this.earthAge * (1/0.62)).toFixed(1));
    // this.marsAge = parseFloat((this.earthAge * (1/1.88)).toFixed(1));
    // this.jupiterAge = parseFloat((this.earthAge * (1/11.86)).toFixed(1));
  }
}